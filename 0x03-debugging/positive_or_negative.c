#include "main.h"
/**
* positive_or_negative.c - print positive or negative interger
* @i: number to be checked
* Return: alays 0
*/
void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative\n", i);
	else
		printf("%d id positive\n", i);
}

